import { InputBorderDirective } from './input-border.directive';

describe('InputBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBorderDirective();
    expect(directive).toBeTruthy();
  });
});
