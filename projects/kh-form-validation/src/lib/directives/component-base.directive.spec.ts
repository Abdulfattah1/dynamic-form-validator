import { ComponentBaseDirective } from './component-base.directive';

describe('ComponentBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentBaseDirective();
    expect(directive).toBeTruthy();
  });
});
