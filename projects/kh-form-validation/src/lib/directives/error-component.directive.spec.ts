import { ErrorComponentDirective } from './error-component.directive';

describe('ErrorComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorComponentDirective();
    expect(directive).toBeTruthy();
  });
});
