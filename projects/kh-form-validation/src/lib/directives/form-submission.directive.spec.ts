import { FormSubmissionDirective } from './form-submission.directive';

describe('FormSubmissionDirective', () => {
  it('should create an instance', () => {
    const directive = new FormSubmissionDirective();
    expect(directive).toBeTruthy();
  });
});
